# Core dependencies for the gap-handling wind forecasting pipeline.
#
# IMPORTANT: this file lists the packages the code imports, with reasonable
# minimum versions. It has NOT been generated from your actual environment.
# Before pushing, replace it with your real environment:
#
#   pip freeze > requirements.txt
#
# then trim it down to remove OS-specific or unrelated packages that pip
# freeze includes automatically (e.g. pywin32, notebook extensions).

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
xgboost>=1.7
matplotlib>=3.7
xarray>=2023.1          # for netCDF (D2 buoy lidar) loading
requests>=2.28           # for knmi_auto.py
tensorflow>=2.13         # for the LSTM in run_model_generalisation_aligned.py
